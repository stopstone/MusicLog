<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingStart="6dp"
    android:paddingEnd="6dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iv_recommendation_track_cover"
        android:layout_width="82dp"
        android:layout_height="82dp"
        android:background="@color/gray_400"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/AppRoundedImage" />

    <TextView
        android:id="@+id/tv_recommendation_track_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/iv_recommendation_track_cover"
        app:layout_constraintStart_toStartOf="@id/iv_recommendation_track_cover"
        app:layout_constraintTop_toBottomOf="@id/iv_recommendation_track_cover"
        tools:text="recommendation_track_title" />

    <TextView
        android:id="@+id/tv_recommendation_track_artist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/gray_700"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@id/tv_recommendation_track_title"
        app:layout_constraintStart_toStartOf="@id/iv_recommendation_track_cover"
        app:layout_constraintTop_toBottomOf="@id/tv_recommendation_track_title"
        tools:text="recommendation_track_artist" />

</androidx.constraintlayout.widget.ConstraintLayout>